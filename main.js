function My_First_Function() { 
    var sentence ="This is the beginning of the string"; //creating variable
    sentence +="And this is end of the string"; //+=operator to add to the variable
    document.getElementById("Concatenate").innerHTML=sentence; //using document.getElementById()
}